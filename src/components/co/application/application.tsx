import { Stack } from "@mantine/core";

export const CoApplication = () => {
//   const { data, isPending, isError } = useGetApplications();

  return (
    <Stack>
      {/* <ApplicationTable
        isError={isError}
        isLoading={isPending}
        applications={data?.data.data.applications}
      /> */}
    </Stack>
  );
};
